enum LocalStorageKey {
  userId,
}

enum Status {
  initial,
  loading,
  success,
  failure,
}

enum PaymentMethod{
  cash,
  online,
}

enum PopupMenuItemOptions {
  edit,
  delete,
}

enum FormStatus{
  add,
  update,
}