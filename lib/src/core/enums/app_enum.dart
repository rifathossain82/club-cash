enum LocalStorageKey {
  token,
}

enum Status {
  initial,
  loading,
  success,
  failure,
}

enum PaymentMethod{
  cash,
  online,
}

enum PopupMenuItemOptions {
  edit,
  delete,
}