enum LocalStorageKey {
  userId,
  isEnableSendSMS,
  messageTemplate,
}

enum Status {
  initial,
  loading,
  success,
  failure,
}

enum PaymentMethod{
  cash,
  bkash,
}

enum TransactionType{
  cashIn,
  cashOut,
}

enum PopupMenuItemOptions {
  edit,
  delete,
}

enum FormStatus{
  add,
  update,
}